import MenuLayout from '../../../layouts/MenuLayout'

const PeopleMenu = (props) => {

  return (
    <MenuLayout {...props}>
      <p>People Menu</p>
    </MenuLayout>
  )
}

export default PeopleMenu